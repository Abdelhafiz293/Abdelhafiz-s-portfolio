{
  "buildCommand": "ng build",
  "outputDirectory": "dist/front-end",
  "devCommand": "ng serve --host 0.0.0.0 --port 4200",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}